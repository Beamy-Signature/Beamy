export default function Loading() {
  return (
    <div className="flex min-h-[50vh] items-center justify-center">
      <p className="text-[11px] tracking-[0.28em] text-muted uppercase">Loading</p>
    </div>
  );
}
